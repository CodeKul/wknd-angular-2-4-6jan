import { CustStrDirective } from './cust-str.directive';

describe('CustStrDirective', () => {
  it('should create an instance', () => {
    const directive = new CustStrDirective();
    expect(directive).toBeTruthy();
  });
});
