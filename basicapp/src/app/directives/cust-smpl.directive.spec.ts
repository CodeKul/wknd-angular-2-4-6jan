import { CustSmplDirective } from './cust-smpl.directive';

describe('CustSmplDirective', () => {
  it('should create an instance', () => {
    const directive = new CustSmplDirective();
    expect(directive).toBeTruthy();
  });
});
